<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.model.dao.UserDAO">
	
	<insert id="insertUser" parameterType="User" >
		insert into user(userId,passWord,name,phoneNumber) values(#{userId}, #{passWord}, #{name}, #{phoneNumber})
	</insert>
	
	<select id="login" resultType="String" parameterType="map">
		select name from user where userId = #{userId} and passWord = #{passWord}
	</select>
	
	<update id="updateUser" parameterType="User">
		update user set passWord=#{passWord}, name = #{name}, phoneNumber = #{phoneNumber} where userId = #{userId}
	</update>
	
	<delete id="deleteUser" parameterType="String">
		delete from user where userId = #{userId}
	</delete>
	
</mapper>














